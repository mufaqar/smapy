/* eslint-disable */

// yarn run-admin-command --cmd scratchPad
import * as z from "zod";

import { value UserProfileModel } from "../../../prisma/zod";
import { value AdvisorUpdateSchema } from "../../components/advisor/advisor-registration-flow/advisor-registration-flow-schema";

export const scratchPad = async () => {
  console.log(`muly:scratchPad v2`, {});
};
